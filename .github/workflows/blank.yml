#name of the workflow
name : first workflow
# when this should run?
on: workflow_dispatch
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World"
      - name : Print Goodbye
        run: echo "Logging out...."
